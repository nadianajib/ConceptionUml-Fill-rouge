package com.example.demo3.model;

import com.example.demo3.model.Personne;
import jakarta.persistence.Entity;

@Entity
public class Admin extends Personne {
    // Les méthodes CRUD seront implémentées dans le service
}